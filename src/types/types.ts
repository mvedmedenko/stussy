
export type addressData = {
    city: string
    country: string
    firstAddress: string
    firstName: string
    id: string
    isDefault: boolean
    lastName: string
    postalCode: string
    secondAddress: string
    stateOrProvince: string
}

export type ProductDetails = {
    amount: number;
    collection: string;
    color: string;
    details: string[];
    firstImg: string;
    id: string;
    images: string[];
    price: string;
    secondImg: string;
    size: string;
    title: string;
  };
